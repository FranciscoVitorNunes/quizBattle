extends Node

var is_dialog_open = false

# Variável global para armazenar a posição de entrada
var entrance_position: Vector2 = Vector2(0, 0)

func _ready():
	# Faz o script ser singleton (globalmente acessível)
	pass
